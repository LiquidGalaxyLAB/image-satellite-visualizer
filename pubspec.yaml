name: image_satellite_visualizer
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  google_maps_flutter: ^2.0.6
  mask_text_input_formatter: ^2.0.0
  http: ^0.13.3
  path_provider: ^2.0.2
  geodesy: ^0.4.0-nullsafety.0
  ssh:
    git:
      url: https://github.com/octaviomuller/flutter_ssh
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0
  build_runner: ^2.0.4

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/examples/
    - assets/json/demos.json
    - assets/demos/